#include "stdafx.h"
#include "CppUnitTest.h"
#include "LinkedList.h"
#include "Snowflake.h"
using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace Testing
{
	TEST_CLASS(LinkedlistTest)
	{
	public:
		
		TEST_METHOD(TestLinkedlist)
		{
			//CLinkedList list;
			//CSnowflake flake;
			//flake.SetPosition(10, 15);
			//Assert::IsTrue(10 == flake.GetPositionX());
			//Assert::IsTrue(15 == flake.GetPositionY());
			//list.Insert(flake);
			//CSnowflake temp = list.Popfront();
			//list.Insert(flake);
			//list.Insert(flake);
			//list.Insert(flake);
		}

	};
}